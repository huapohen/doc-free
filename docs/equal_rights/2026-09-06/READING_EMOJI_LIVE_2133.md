# 未读、话题、回执与表情真实联调 · 2026-09-06 21:33

记录时间：2026-09-06T21:33:00+08:00，Asia/Shanghai。分支：`equal_rights`。

代码基线 `b8747976dc37a68eedd199a76054a33a53a1d978`（2026-09-06T20:35:03+08:00），文档基线 `38b65df37f8f25f0c4506d1a052c26a0d50273cc`（2026-09-06T20:42:00+08:00）。本批新实现写入本记录时尚待汇总 commit，最终交付须列实际版本。协议详见 `READING_THREADS_EMOJI_2123.md`。

父任务把 HTTP 3218 / CRDT 1238 服务重启到包含清空最近使用的新源码，使用 `--no-worker`。后端专项仅连接本机公开成员 HTTP/MCP 接口，读取已有凭据且不输出完整凭据；未调用模型、创建新身份或操作真实飞书。测试脚本及完整脱敏证据位于 Active 的 Git ignored `output/reading-emoji-live.py` / `output/reading-emoji-live.json`。

## 结果

纠正测试身份角色假设后，真实专项 **424/424 检查通过**。检查数包含 HTTP 状态、MCP 返回状态和语义断言，不将其称为 424 个独立测试用例。后端全量源码测试 **273/273 通过，0 失败、0 跳过，14.22 秒**；表情定向 **7/7**、消息阅读定向 **9/9**。

真实 Human 为 huapohen，真实 Agent 为 activate-agent。两者各创建一个独立、明确命名的本地测试群：

| 群 | 会话 ID | 消息 |
| --- | --- | --- |
| 阅读与表情原生联调 · human群主 · 2026-09-06 21:32:41 | `room-b9bda5ab-33cb-4736-a8d2-10db14da6dd3` | 209 |
| 阅读与表情原生联调 · agent群主 · 2026-09-06 21:32:41 | `room-6f920d65-062e-486a-bcb6-f5a869ea591b` | 9 |

两群的 Agent 均为 paused，持久状态中 turns 为空。所有消息 ID、ACK 证据和 paused 设置均检查了磁盘实际记录。测试群作为可复查证据保留，不混入公司正式交付文档。

## 验证范围

- 205 条连续未读使第一条未读落在详情最近 200 条之外；`first_unread` 仍精确返回真实首条，局部显式 ACK 仅推进两条，剩余未读数和首未读正确。after 独占游标和 around 定位与真实消息序号一致。
- Human/Agent 均经 HTTP/MCP 操作窗口、话题和 readers。GET 和 MCP 读取不写 ACK，显式 `im_preferences read_seq` 后收据才显示 read。测试结束保留 ACK，不回滚阅读证据。
- 递归话题读取真实 reply_to：首回复、嵌套回复、同级回复分页正确；不创建话题占位，进入话题不推进阅读水位。
- 双方尚未受邀进入对方测试群时，消息窗口、thread、readers 和 MCP thread 均返回当前非成员 403；受邀后获得同权成员能力。晚加入不改变此前消息的空收件人快照。
- 两身份均可通过中文、英文、经典 ID 和复杂 Unicode 序列搜索；完整目录逐页读取共 4126 个唯一 ID，与固定 JSON 顺序完全一致。经典分类共 182 项。经典条目的 text token、asset 相对路径和本地 PNG 签名验证通过。
- Human/Agent 均通过真实 MCP 添加复杂肤色/ZWJ 序列回应，通过 HTTP 添加 `feishu:SMILE` 回应，消息记录保持精确 ID。输入选择与回应使用同一最近列表；取消回应不把其移到最近首位。
- 个人最近列表的跨身份字段写入被拒绝；本人最近更新不改变另一身份列表。MCP 清空只清本人最近记录，消息上的实际经典回应继续存在。
- 两身份测试前最近列表均为空，结束经本人 DELETE 恢复空列表，再 HTTP 读回确认。更新时间正常推进，不直接编辑运行数据文件。没有改动既有导航、静音、折叠或其它个人设置。

## 组织与权限证据的实际含义

核对 `/me`、`/enterprise` 与持久企业成员引用：huapohen 的组织为“人机共创公司 · 演示”；activate-agent 的组织和部门为空，接口没有把目录来源单位伪造成实际组织。组织名称来自真实引用，管理接口访问依据当前企业 role。整个验证前后企业组织、成员、层级和策略的序列化内容保持一致。

首次脚本把现有 activate-agent 误当作企业普通成员，预期管理组织接口 403；实际该身份拥有管理角色，返回 200 是正确行为。该次运行停在只读阶段，没有建群、发消息或修改最近列表。完整失败检查保留在证据 `initial_script_attempt`，随后脚本按 `/enterprise` 真实 role 判断预期状态重跑。没有为让测试通过而降权、提权或改服务权限。

本次验证的是组织字段真实、权限由真实角色决定，以及会话成员边界。该运行环境是单一企业工作区，不声称 organization 字段已经形成独立多租户隔离。

## 图片部署验证边界

424 项成员 API 及本地图片资源检查完成时，补查 `/office/assets/assets/emoji/feishu/SMILE.png` 返回 404，说明运行中的 Flutter 静态构建尚未包含新图片。此时只确认源文件 PNG 与接口目录正确，不把它写成线上图片已加载。父任务正在准备最新 Flutter 构建；图片 HTTP、选择器及渲染画面须在构建落地后补充单独证据。

### 21:44 图片构建落地补充

父任务完成最新 Web 构建（`/tmp/office-web-build-2142.log`）后，运行独立只读脚本 `active_agent/output/emoji-assets-http.py`。**182/182 张经典图片 HTTP 验证通过**：状态均为 200，Content-Type 均为 image/png，PNG 签名及 IHDR 有效，HTTP 返回字节与源码文件完全一致，逐项 SHA-256 相同；合计 1,880,966 字节。

完整逐图 ID、URL、SHA、字节数和尺寸证据为 `active_agent/output/emoji-assets-http.json`。请求未携带成员凭据，未修改任何服务状态。早先旧静态构建的 404 在该证据 `earlier_probe` 保留；新构建的 182 项均通过。图片静态资源部署已验证，客户端选择器及实际画面继续以父任务 UI 验证为准。
