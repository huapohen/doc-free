# 手机消息分组完整面板：参考纠正与布局校准

记录时间：2026-09-07 11:42:35 +08:00。分支：`equal_rights`。

本轮开始时 HEAD：`02f740fd39645e15e199e3d635da6cb35c772442`，时间 2026-09-07T11:07:43+08:00，描述 `docs(office): record native group toggle and stable input verification`。本文件记录该基准之后的工作区增量，最终实现提交由主集成交付文档记录，不能把上述历史提交当作本轮实现 SHA。

## 先纠正上一轮参考判断

用户提供的完整并排截图显示：原人机手机分组的字号、行距、左右留白与飞书差异明显，抽屉上下安全区还是灰色。此前只比较标题栏的局部验收不足以证明完整分组面板一致。

主任务本轮重新单独点击飞书手机右上角图标，再读取下一帧，确认该图标是“列表与齿轮”的**编辑分组入口**：先关闭抽屉，再打开编辑页。它不是收起抽屉按钮。`GROUP_HEADER_TOGGLE_1053.md` 和 `GROUP_HEADER_NATIVE_QA_1100.md` 中相应手机按钮用途及“点击标题收起”的结论被本次实看纠正；旧测试通过只证明当时实现满足旧断言，不能证明旧断言符合真实产品。

## 视觉依据与视口

主要视觉真值为用户提供的 `automation/2026/04_09/3/05/input/img/5.png` **右侧真实手机**，左侧旧模拟器是待修实现，不是目标。源文件位于仓库上层工作区。本轮不保存或发布真实会话内容。

| 来源 | 屏幕裁剪范围 | 裁剪尺寸 | 对齐尺寸 |
| --- | --- | --- | --- |
| 用户 5.png 右侧真机 | `[654,14,1257,1324)` | 603×1310 | 402×874 |
| 第一轮人机原生 `output/renji-mobile-panel-first-native-1137.png` | `[22,75,315,714)` | 293×639 | 402×874 |

以上原生模拟器截图窗口被缩小，屏幕裁剪坐标按主任务回传的可绘制范围处理；没有将窗口尺寸或截图像素当成 Flutter 逻辑像素。主任务确认 iPhone 17 display 为 1206×2622，对应 3 倍密度的 402×874 逻辑视口。两个屏幕裁剪分别缩放到这一视口后比较，保留原有系统圆角和状态栏差异，不重绘设备外框。

第一轮完整对比：`output/message-group-mobile-full-compare-first-1139.png`。对应单屏归一文件：`output/message-group-reference-402-1139.png`、`output/message-group-native-402-first-1139.png`。暴露的右侧聊天区域已遮盖；水印及用户对比图中的红字保留在本地参考文件中，不纳入提交、不嵌入公开文档。

早期 `output/feishu-mobile-group-panel-reference-1115.png` 可用于内容顺序核对，但其 318 px 原截图含外部留白，不能用整个窗口宽度推算手机正文的逻辑字号。此处使用用户指定的右侧完整屏幕裁剪作为尺度依据。

## 两轮校准

第一轮先解决白底覆盖安全区、圆形消息图标、标签默认折叠、无常驻加号、去除手机逐行重复计数，并将手机正文独立为 16 号字与 48 行高。原生截图随后显示：标题与内容整体偏右约 7–8 个归一像素、第一行下移约 11、面板宽度少约 8；标题字形已接近参考，正文仍略小。

第二轮据完整图调整：

| 项目 | 第二轮实现 |
| --- | --- |
| 抽屉宽度 | 视口 80%，保留 240–350 逻辑像素边界 |
| 背景 | `showDialog(useSafeArea:false)`，白色 Material 覆盖全高，内容在其内部 SafeArea 中 |
| 标题 | 20 号、半粗；header 内边距左 26、上 4、右 12、下 5 |
| 正文 | 17 号、行高系数 1.25，未选中灰色、选中蓝色 |
| 列表 | 左 10、右 8；行内左 14、图标列 24、图标与文字间距 12 |
| 行节奏 | 每行 49；可见选中底高 47，上下各留 1 |
| 选中底 | 浅蓝 `#e9efff`，圆角 6 |
| 标签 | 手机首次打开收起；三角与文字对齐，无固定加号和空态说明占行 |
| 桌面 | 160 宽及既有 header/开关坐标保持；新增真实类别正常读取 |

参考图与第一轮原生图的选择分别为“消息”和“标记”。这可以定位列宽、文字锚点和各行节奏；同选择状态的最终截图仍由主任务补验，不把这一差异当作业务错误，也不通过打开时重置个人选择来伪造一致。

## 真实交互与数据

手机右上角管理入口现在返回路由结果，等待旧 drawer overlay 完全退出，再打开分组编辑页。返回编辑页前重新检查端点、主体与身份代次。旧抽屉的关闭、编辑回调不能关闭新编辑页或新抽屉；退出编辑回到消息页，重新打开入口仍可用。

当前服务端 `labels` 容器按真实 `order` 出现在顶级列表，展开时按服务端顺序显示子标签，容器隐藏时不泄露子项。`documents` 和 `topics` 仅在服务端实际返回时显示，点击继续使用 `room_ids` 与本人已授权会话取交集。旧端点尚未提供容器 ID 时，保留此前的虚拟标签区兼容展示；不向旧接口写入虚构 ID。

图标使用官方图标库源：Remix Icon v4.6.0 / Apache-2.0、Phosphor Icons / MIT、Solar / CC BY 4.0 和 Tabler / MIT。源内容核对 Git blob SHA 后提取原始 SVG，转为透明 PNG，没有新运行时依赖；源路径、SHA-256、逐 asset 映射和许可证位于 `apps/office/assets/message_groups/`。单聊/群组采用已有 Flutter Material Icons（Apache-2.0）。授权库图形仍可能与飞书细部轮廓不同，不声明所有图标逐像素一致。

## 验证与当前边界

| 检查 | 结果 | 日志 |
| --- | --- | --- |
| 第一轮分组/折叠联合回归 | 43/43 通过 | `/tmp/renji-mobile-group-panel-tests-1136.log` |
| 第二轮 panel 专项 | 18/18 通过 | `/tmp/renji-mobile-group-panel-tests-1144.log` |
| 第二轮三个所属文件分析 | No issues found | `/tmp/renji-mobile-group-panel-analyze-1144.log` |

测试包含桌面同坐标开合、手机管理迁移、遮罩/返回/选择退出、390 与 402 宽白色安全区、标签默认折叠、新版容器排序及隐藏、文档与话题真实集合筛选、同身份重开及旧回调/切换身份保护。测试未替代原生截图验收。

第二轮原生重建与完整同状态截图对比仍由主任务继续完成，12:05 再次确认 Mac 系统锁屏，尚无法完成最终原生实点和截图。此前原生热加载过程遇到旧 asset bundle 引用已清理图标，主任务已改为同一模拟器重新 `flutter run` 生成完整 bundle；最终图标以新路径追加，未删除或移动现有图标资产。最终原生结论保持 pending。

## 最终收尾与冻结

最后源码改动时间：2026-09-07T12:08:57+08:00。分组标题、列表尺寸和交互代码已冻结。最终实现提交 SHA 仍由主任务主交付文档补入，本记录不提前编造提交号。

标签展开后尾部恢复可发现的“新建标签”，包括零子项的情形；点击先退出分组抽屉，再打开实际创建对话框。新增回归覆盖输入标签名、提交创建、重新展开后显示新标签，避免移除常驻加号时把真实创建入口一起丢掉。

1155 widget 并排图暴露原图标的显眼差异后，改进未读/完成为 Solar 圆气泡，标记为 Tabler `flag-3` 三角缺口旗，话题为 Tabler `message-2` 文本气泡。单聊/群组换为现有 Material 平底胸像，并将 glyph 调至26以补偿图形自身内缩；行高、图标列与文字坐标不变。来源库版本、作者、许可证和每个 app asset 的映射都在资产 README/manifest 中，旧PNG和旧source条目保留。

最终捕获为 `output/message-group-widget-render-402-1212.png`，来自生产 Flutter widget 树，402×874视口、安全区62/34、3倍渲染，输出1206×2622。显式加载本机 PingFang SC、SFNS 和 Flutter MaterialIcons，修复默认Ahem导致的错误字体；系统字体只本机读取，没有随项目分发。该捕获仍是widget验证，不能替代iOS原生文字光栅化和系统状态栏。

最终完整并排图 `output/message-group-mobile-full-widget-compare-1212.png` 与局部并排图 `output/message-group-mobile-focused-widget-compare-1212.png` 已实际打开核对。局部取两侧屏幕 `[0,62,322,622)`，排除系统chrome与用户红色批注；全图保留并注明这些差异。`1155`、中间`1210`和第一轮原生证据均保留，未覆盖旧图。

残差为授权库描边、未读空心圆点、完成勾与闭合气泡轮廓、单聊/群组胸像细节、云文档折角和话题气泡角等P3；不声称逐像素一致。当前全高白底、字号、行距、图文锚点和内容顺序的P1/P2已在widget复验中改善；原生最后一轮因Mac锁屏仍未验收。

| 最终检查 | 结果 | 日志 |
| --- | --- | --- |
| 分组开合、分组功能、折叠会话3文件专项 | 44/44通过 | `/tmp/renji-mobile-group-panel-tests-1212.log` |
| 所属源码与测试共5文件分析 | No issues found | `/tmp/renji-mobile-group-panel-analyze-1212.log` |
| 最终widget捕获 | 1/1通过，不混入业务测试计数 | `/tmp/renji-message-group-render-1212.log` |

项目根 `design-qa.md` 已切换为本轮手机分组报告，明确 `final result: blocked`（原生复验阻断），旧根QA原样保存至 `DESIGN_QA_BEFORE_MOBILE_GROUP_PANEL_1205.md`。全量测试与各端构建由主任务统一记录。

本文件不承担编辑分组 sheet 内部细节的验收，见 `MESSAGE_GROUP_NATIVE_EDITOR_1131.md`。当前不声明完整飞书办公能力、全部手机后台或五端原生验收已完成。
