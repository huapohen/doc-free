# 人机输入区 · 截图校准记录

本记录只评估 2026-09-07 插入的输入发送区校准，不代表全量飞书功能或全应用视觉验收。最终提交与时间将在本轮交付文档中记录。

## 参考与归一化

- 桌面视觉真值：`/Users/lwblx/huapohen/agent/automation/2026/04_09/3/05/input/img/3.png`，976×196 像素。
- 用户提供的修改前人机：同目录 `4.png`，952×260 像素。
- 飞书边框区域 `(16,24)-(956,188)`，940×164 像素；按 2× 密度假设对应 470×82 逻辑像素。原图没有附带系统缩放参数，因此这里明确标注推断，不把它写成已查询的设备密度。
- 旧人机边框区域 `(20,8)-(914,242)`，894×234 像素，按相同密度假设为 447×117。两图原始输入区宽度不同，不通过横向拉伸伪造一致的文字和图标尺寸。
- 同一比较输入：`output/composer-reference-before-compare-0907.png`。已实际打开并排图核对，非分别读图后凭记忆评估。
- 手机来源：实际操作 iPhone Mirroring 中的飞书，空态、Aa 格式栏、全屏展开和收回均已观察；本地裁剪在 `output/feishu-mobile-composer-{empty,format}-0907.png`。镜像缩放与 iPhone 17 模拟器设备框不同，需先按应用内容区归一化，不能对整机截图直接报像素相似率。

## 第一轮发现与待复验

| 严重度 | 位置 | 实际差异 | 修复方向 |
| --- | --- | --- | --- |
| P1 | 桌面框体 | 旧人机约 117 高，参考约 82 高；占据更多聊天空间 | 默认空框按 470×82 组件视口测量，输入增长仍可扩展 |
| P1 | 桌面工具行 | 旧工具左对齐、原始附件/文档/任务常驻，参考工具靠右 | Aa、表情、提及、截图、更多、展开、发送方式按参考排列；加 Agent 专属入口 |
| P2 | 发送 | 旧版大蓝色文字按钮及快捷键文字占据底栏 | 图标发送与独立下拉；空消息禁用；快捷键选择必须实际影响发送行为 |
| P2 | 字体与文案 | 旧占位更粗，且为产品解释性文案 | 使用“发送给 {当前会话}”，对照字体大小、字重与灰色 |
| P1 | 手机 | 旧整块输入卡和文字发送按钮不符合实际手机参考 | 白色小输入条、独立工具行；空态隐藏发送，保留 Agent 入口 |

## 五项视觉检查

- 字体：检查中文平台字体、字重、字号、行高与占位文字；组件截图必须加载真实字体，不能用 Flutter 测试默认 Ahem 代替。
- 布局：框体高度、圆角、内边距、工具间距、右对齐、展开状态、键盘后的可用空间。
- 色彩：边框、输入背景、工具灰色、禁用发送、交互蓝色；以源裁剪与实现裁剪同图检查。
- 图像：这一局部没有待生成的插图或品牌图片；使用现有标准图标库。Agent 图标是明确的产品增量，不冒充飞书原有按钮。
- 内容：保持会话名称占位和有意义的功能名称；富文本按真实样式渲染，截图使用原生选区，未支持的语音能力不伪装为可录音。

## 最终同状态对照与复验

2026-09-07 10:24（Asia/Shanghai）复验。主协作者与独立视觉协作者均实际打开最终桌面并排图 `output/composer-reference-final-compare-0907.png`，以及手机归一化图 `output/composer-mobile-icon-normalized-1022.png`。不是按两张图的记忆判断。

| 项目 | 最终测量或实点结果 |
| --- | --- |
| 桌面空态框 | 940×164 像素（470×82 逻辑尺寸，2×渲染），与参考裁剪一致 |
| 占位文字 | 参考墨迹框 `(28,35)-(256,61)`；实现 `(28,35)-(257,60)`；主灰色均为 `#929494` |
| 桌面右端按钮 | 更多、展开、发送、下拉中心与参考相差约0.5像素；工具行中心115px，对照115.5px |
| Agent 增量 | 增加专属入口，占一个额外槽位；不声称整张位图完全一致 |
| 手机工具图标 | 以可编辑内容宽度归一化，emoji墨迹约18.125×18.125px，参考约18×17px；修复了此前明显偏小的P2 |
| 展开页工具 | 增加与减少缩进均存在，保留 Agent 超级入口；320逻辑宽度加键盘避让专项无裁切 |
| 手机原生操作 | 实际打开软件键盘、Aa、全屏展开、收回、再次展开、发送；标题未重复并在服务端保留真实粗体 |
| 手机面板切换 | 加号二次点击收起；普通及展开页 Agent 按钮均实际打开完整协作面板并可返回 |
| 桌面原生操作 | 最新原生构建后用既有账户登录；日程/会议表单打开、取消、回聊天；合并转发成功并读完整记录 |

最新原生截图为 `output/ios-composer-empty-native-final2-0907.png`、`output/ios-composer-expanded-native-final2-0907.png`、`output/ios-composer-keyboard-native-final-0907.png` 和 `output/macos-composer-native-final-0907.png`。它们包含不同设备的系统边界，不能对整个设备图片拉伸后报告“像素完全相同”。组件捕获使用实际 Flutter 渲染、平台字体及 Material/Cupertino 字体，不使用 Ahem 或以设计图片替代组件。

## 五项检查结论

字体、布局、色彩、图像/图标与内容均已重新对照。该输入区的阻断性布局差异已修复；待保留的P3是库图标轮廓（emoji、@、剪刀、加号）、Aa光栅化字重、手机hint灰度与格式栏收起按钮底色的小差异。它们被明确列出，不称为飞书原有素材的逐像素复制。

## 验收范围与真实限制

本结论只覆盖本轮输入区的可见状态、草稿/排版、已实点导航和真实发送，不是完整飞书办公能力验收。麦克风按钮仍真实禁用，语音输入/消息尚未接入；原生截图按钮可以启动macOS系统选区，但操作工具无法驱动这个全局选区，已结束该次选区进程、恢复客户端并验证错误反馈，未把真实截图附件上传算作通过。其他端系统截图不伪称支持。

镜像本轮曾恢复并实际检查飞书空态、Aa与展开；之后重连又要求Mac系统密码，已请求用户亲自解锁。已有参考截图和最新模拟器截图可用于本次比较，因此镜像后续锁定不再阻断已经有同状态参考的局部验收；它仍阻断新的真实手机参考操作。没有修改系统隐私权限或假称镜像不会断开。

## 10:59 原生输入稳定性补验

用户在前次局部验收后发现，手机输入文字时动态出现的发送按钮会让整行重新分配间距。真实飞书手机再次实点确认普通/Aa工具行都不插入发送；修复采用系统键盘发送，保留固定工具行和 +。实现提交 `c28c5422fa67b5b6490d206f3f05b26a70e9286d`，时间 `2026-09-07T10:59:56+08:00`，描述 `fix(office): relocate group toggle and stabilize mobile composer tools`。320/390、键盘开关、普通/Aa的8条位置回归先全部复现失败，修复后全部通过；主整合最终662/662与analyze通过。

实际打开 iOS 原生 1051/1052 截图的同坐标工具行裁切后，普通空→输入的13,200像素完全相同，7个图标墨迹中心位移0px；普通空→清空排除鼠标及阴影后比较11,280像素，变化0，6个未遮挡图标位移0px；Aa空→输入排除鼠标及阴影后比较10,160像素，变化0，6个未遮挡图标位移0px。emoji和下划线在各一张图中被鼠标遮住，明确不借该对原生截图证明这两个位置；未遮挡普通空→输入可以完整验证7项。

证据为 `output/mobile-native-toolbar-stability-1057.png`、`output/mobile-feishu-footer-reference-1057.png` 和 `output/mobile-native-toolbar-metrics-1057.json`。只呈现输入footer与工具行，不包含私人聊天；像素稳定性计算没有缩放或配准，飞书参照展示单独归一到290px输入宽度。含指针原图有1,844/2,615变化像素，因此不声称整张原图一致。

原生键盘也完成实际发送、消息到达、草稿清空和焦点保留。开发hot reload若旧TextField保持focus，Flutter3.47.2可能保留旧newline输入连接；退出会话再进入后新连接正确显示蓝色发送键并成功发送，见 `MOBILE_NATIVE_KEYBOARD_VERIFICATION_1052.md`。没有为了这个开发边界更改业务发送或增加工具栏按钮。

该追加验收仍保留前文P3字体/库图标/颜色差异与未实现能力，不升级为全量飞书复刻完成。

final result: passed

仅指上述局部输入区视觉QA通过。全量IM能力、语音、所有终端、生产签名、公证和真实外设控制保持未完成状态；最终Git提交、测试/构建和共享文档读回单独记录在本批日期交付文档中。
