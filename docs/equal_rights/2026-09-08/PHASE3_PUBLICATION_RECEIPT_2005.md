# 日历与会议阶段协作文档发布回执

记录时间：2026-09-08T20:05:17+08:00。分支：equal_rights。
实现 commit：`bd4ade752513c8b14cce7b542f5439a3e33fef24`。
实现时间：2026-09-08T19:55:32+08:00。
描述：`feat(office): refine calendar and meeting native interactions`。

## 发布结果

已在“人机共创公司 · 全员协作”创建一份新的共享文档，未覆盖历史交付：

- 标题：日历与会议阶段验收 · bd4ade7 · 2026-09-08 19:55。
- 文档 ID：`67bca2fd`，版本：`r1`。
- 会话 ID：`room-653437e5-042e-43d0-aa99-0e7b744e6e00`。
- 仓库正文：[PHASE3_NATIVE_CLOSEOUT_1955.md](PHASE3_NATIVE_CLOSEOUT_1955.md)；提交前仅规范了文件末尾换行，详见下文。
- 首次发布记录时间：2026-09-08T20:01:36+08:00。
- 后续只读核验时间：2026-09-08T20:04:47+08:00。

在已登录的 macOS 人机客户端，点击“云文档”首条可见该标题。已实际打开，确认共同版本 r1、实现完整 SHA、时间及正文开头。未点击编辑保存。原生截图保存在 Git 忽略的 `output/phase3-resumed-native-20260908/renji-desktop-stage-document-published.png`。

## 正文归一化与原始失败记录

只执行一次创建 POST，没有重试或创建第二份文档。首次回读断言只允许删除一个末尾换行，而源文件实际以两个 LF 结尾；服务端创建契约移除了这两个终端 LF，因此该断言失败。原始回执保留 `all_passed:false` 和失败项，不改写为成功。

随后仅使用 GET 独立核验，确认正文只少了这两个末尾 LF，其他字符完全一致，服务端内容哈希也与完整回读相符：

- 冻结源文件 SHA-256：`46c14c39ca4788a923e262c79ac8ad8415f2ae7fa6b89e31fb60a303bf0b3ec4`。
- 共享正文 SHA-256：`c1a194e5ade9452440315f76081a2c86f5499d104405656d1bc4b2f2bc0070e7`。

两者不是字节完全相同；该差异有精确解释。共享正文未被再次修改。

提交前的暂存区检查发现仓库文件多余的末尾空行。为通过 `git diff --cached --check`，两仓正文仅删除一个终端 LF，保留标准单个文件末尾换行；没有修改正文文字，也没有重新发布共享文档。仓库文件 SHA-256 为 `93bf453ea79c1fb4dd0d8fd07fda37497930cab5d6f4ffbcf36d8f622ddeb702`。发布时的原始字节完整留存在 `output/phase3-calendar-meetings-delivery-document-20260908-source.md`，哈希仍为上述冻结源哈希。原始 JSON 的 source 路径是发布时来源，不代表仓库格式整理后的现时哈希。

## 权限与保全核验

独立只读核验 **41/41 通过**。huapohen、公司管理员、普通员工及公司 Agent 均能读取同一完整正文；非会话成员 Agent 被拒绝，跨会话读取被拒绝，均返回 403 和对应的权限错误。

公司会话从 16 份文档增至 17 份，原 16 份的标题、版本和内容哈希均与发布前一致，包括前次交付 `e78eb7f5`。四个已有身份的登录会话数量与发布前一致。

发布器只有一条受限的文档创建写路径，不发送聊天消息，不写入成员或权限。首次核验中断前的完整成员、会话和消息快照仅在内存中，未持久化；后续只读核验没有把它们冒称为已独立逐字段比较。文档保全和登录会话数量具有持久化的前后依据。

本机审计文件（均在 Git 忽略目录，无凭据正文）：

- `output/phase3-calendar-meetings-delivery-document-20260908.py`：唯一创建发布器。
- `output/phase3-calendar-meetings-delivery-document-20260908.json`：原始发布回执，保留末尾换行断言失败。
- `output/phase3-calendar-meetings-delivery-document-20260908-readback.json`：仅 GET 的独立核验回执。

阶段源码有 166 项相关 Flutter 回归通过，静态分析无问题；发布核验独立于 UI 回归。电脑端与模拟器保持登录，停止继续操作供用户验收。全量飞书复刻、所有平台验收和系统级人工重复输入问题仍未全部完成。
