# equal_rights 继续交付记录

记录时间：2026-09-08T13:29:51+08:00（Asia/Shanghai）
分支：`equal_rights`

## 本次提交

- Active Agent：`2dc1abcd2665f9c3a58f2111cf6d7d3bdb3caca6`
- Doc Free：`d0be30a3c08920642ff7f94c4d0d52b9450c5e72`
- 描述：继续补齐飞书办公 IM 的桌面/移动交互，并将人和 Agent 置于同一权限、消息和工作台协议；同时加入可复现的本地启动教程。

## 已确认的行为

1. Mac 端重新构建并运行后，左上头像为圆形；头像旁“新建与添加”打开带图标的白色菜单，菜单能在按钮下方/右侧定位，重复点击可关闭。
2. iPhone 17 模拟器重新构建并运行后，以本地工作身份进入消息页；消息分组、置顶会话头像架、未读/已读信息、Agent 入口和长按菜单保持可见。
3. 会话右键/长按菜单支持置顶、清除未读、标记、标签、免打扰、完成，并额外提供 Agent 超级入口；置顶写入个人 `pinned` 偏好并可从头像架打开原会话。
4. 企业管理入口现在同时检查服务端能力和 membership 状态；停用或撤销身份不会因旧摘要残留而显示管理入口，管理员与普通成员的角色/状态在身份面板中可见。
5. 工作台最近使用记录走身份隔离、持久化 REST/MCP/A2A 合同；语音附件按真实 PCM16 WAV 字节校验。人和 Agent 共用同一套服务边界。

## 验证

- Flutter `flutter analyze`：通过。
- Flutter 全量测试：`827` 项通过。
- 重点 UI/交互测试：`59` 项通过。
- Doc Free Node 测试：`364` 项通过。
- `git diff --check` 与敏感字段扫描：通过。
- 本地服务健康检查：`GET http://127.0.0.1:3218/health` 返回 `{"ok":true}`。

## 手动启动

详见同目录 [MANUAL_STARTUP_MACOS_IOS_SIMULATOR_1318.md](./MANUAL_STARTUP_MACOS_IOS_SIMULATOR_1318.md)，其中包含服务、Mac、iPhone Simulator、热更新、端口和断网排查命令。教程不保存密码、API Key 或访问令牌。

## 边界

当前交付仍不是所有飞书页面的像素级完成版。Windows/生产签名公证、完整云端部署、扫码/钱包/自定义状态/多账号持久化、真实 ASR/TTS、真实会议媒体和第三方企业连接仍需后续实现或真人验证；未把这些项目标为已完成。
