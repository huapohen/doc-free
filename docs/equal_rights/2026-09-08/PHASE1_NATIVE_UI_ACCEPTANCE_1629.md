# 人机：第一阶段原生界面验收

记录时间：2026-09-08 16:29 +08:00。分支：equal_rights。

## 实现提交

- `be6f71d167fef20354900ff2d63be8dc94b314ae`，2026-09-08T15:42:38+08:00，`feat(office): align first-stage native mobile and desktop surfaces`：手机消息首页、个人页、更多菜单及桌面菜单的视觉和交互调整。
- `c4942b752a7ddbdc6672ff2da64ba9cb96547bf4`，2026-09-08T16:28:34+08:00，`fix(office): shorten mobile Agent quick action labels`：手机显示“添加 Agent”“Agent 商店”，保留完整无障碍名称、权限检查和动作协议。
- 本阶段没有修改 Doc Free 后端协议；本文同步到两个项目，用于关联验收。

## 本阶段交付范围

手机消息首页在 100% 字号下对齐头像、文字和行高：圆形会话头像 50、标题 17、摘要 15、时间 13；消息行保持约 68 逻辑像素。顶部头像 38，置顶区头像 44；保留真实置顶、时间、免打扰和未读状态。底栏图标 24，高度 74，移除椭圆选中背景。分组页既有字号不改。

个人页采用左侧实际组织栏和右侧个人信息/操作栏，抽屉宽度 87.5%，组织栏 98；头像、标题、信息条、操作行的纵向位置已根据稳定镜像截图调整。管理员入口仍按真实权限显示，未伪造多账号、企业认证或钱包状态。

更多面板改为最近使用、全部/返回、四列应用网格和编辑入口。面板高度按全屏 64% 并受可用区域限制；图标 48。修正顶部状态栏遮罩，并保持底栏可点击：再次点击“更多”会关闭面板。最近使用仍来自真实持久化记录。遮罩使用稳定组件树，避免打开菜单重建当前业务页面。

手机 + 菜单从按钮下方靠右展开，白底图标列表，可滚动；额外保留 Agent 入口。桌面左上头像为圆形，+ 菜单缩为 180 宽、38 最小行高、18 图标，减轻阴影。保留此前已实现的打开 48ms / 关闭 36ms 动画；这不是端到端延迟实测，也不是本阶段新增的速度指标。

## 原生操作与截图证据

iPhone 17 模拟器和 macOS Debug 应用均在已登录状态下查看并热更新。手机实际验证：打开更多→全部→返回→再次点击更多关闭；打开/关闭个人页；打开/关闭 + 菜单。桌面实际打开/关闭 + 菜单。没有向飞书发送消息、修改真实企业权限或输入密码。

本地证据位于 active_agent/output/phase-ui-20260908/，包含：

- messages-comparison-final.png：消息首页对照。
- profile-comparison-final.png：个人页对照。
- more-comparison-final.png：更多面板对照。
- plus-comparison-final.png：手机 + 菜单最终对照。
- renji-desktop-plus-final.png：原生桌面菜单。

截图以内容区归一到 402×874；模拟器裁剪 (22,75,315,714)，镜像裁剪 (8,38,310,694)，不以外框大小判断字体。个人页参考使用 feishu-mobile-profile-reference-stable.png，排除抽屉动画中间帧。参考含真实账号信息，仅保存在被忽略的本地 output，未提交 Git。

## 验证

- 本阶段相关组件和交互测试 97/97 通过，日志 /tmp/renji-phase1-final-tests.log。
- 最后标签调整后 quick_create_menu_test.dart 11/11 通过，日志 /tmp/renji-phase1-label-final.log；两组存在重叠，不累加成测试总数。
- flutter analyze 无问题。Mac、iOS 本阶段开发构建和热更新成功；未据此宣称 Windows/Android/Web 重新构建完成。
- 默认测试字体不代表系统中文字形：单行视觉结论使用原生截图验证，组件测试验证标签与动作。未把测试字体高度当成原生像素误差。

## 尚未完成与验收边界

这是一阶段布局与交互交付，不是全量飞书复刻或逐像素完全相同。账号、会话、组织与安装应用内容不同；图标轮廓仍有差异。Agent 额外菜单增加总长度。扫描、多维表格、问卷、投屏等未接入项目显示真实不可用说明；个人页钱包、二维码、自定义状态、签名等没有冒充实现。人机妙记入口不等于已实现 ASR。

重复输入问题尚未定位根因。用户已确认手动输入以及仅恢复控制连接时正常；本次只点击和截图，未用键盘注入复测，不能据此声称问题彻底修复。

本阶段在此停下供用户验收。建议检查：手机首页字体/头像与置顶区、个人页、更多菜单再次点击关闭、两端 + 菜单。企业后台、完整设置和其余页面作为后续阶段，不在本次完成声明中。
